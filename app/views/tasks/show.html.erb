<p><%= @task.title %></p>
<p><%= @task.details %></p>
<%= link_to "Edit task", edit_path(@task.id) %>
<%= link_to "Delete task", delete_path,
      method: :delete,
      data: { confirm: "Are you sure?" } %>
